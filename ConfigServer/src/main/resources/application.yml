server:
  port: 9296
  
spring:
  application:
    name: CONFIG-SERVER
    
  cloud:
    config:
      server:
        git:
          uri: https://github.com/mk62586/ConfigServer
          clone-on-start: true
          default-label: main
         